# Example environment variables for Currency Exchange Server
# Port the server should listen on
PORT=8090

# Host/IP address the server should bind to. Use 0.0.0.0 to listen on all interfaces
# For example: HOST=0.0.0.0
HOST=127.0.0.1